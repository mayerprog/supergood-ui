import styles from "./AddressContainer.module.scss";

const AddressContainer = () => {
  return (
    <div className={styles.container}>
      <h2>Мои адреса</h2>
      <span>Здесь будут адреса</span>
    </div>
  );
};

export default AddressContainer;
